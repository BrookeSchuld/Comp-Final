import ROOT as RT
from ROOT import TLorentzVector
import numpy as np
import sys
import lar_functions as lar
import math

def characteristics(evt,vtx,part_id,trk):
    masses=np.array([938.27,135.0,497.61]) #proton,pion,kaon
    L=[]
    p=traj[part_id].Points[-2].GetMomentum().Mag()
    reco_energy,de_dx=reco(part_id)
    for mass in masses:
        L.append(plotting_functions.bethe_bloch(p,mass)
    
    
